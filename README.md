#  Demo Auth Server (Node.js + Express + JWT)

Server này được tạo cho mục đích **demo API** sử dụng **JWT Authentication**,  
phục vụ cho các app Android hoặc client khác gọi thử các route `login`, `refresh`, `products`, v.v.

---

## 🧩 Yêu cầu môi trường

- Node.js **>= 18**
- npm **>= 9**

Kiểm tra phiên bản:
```bash
node -v
npm -v
```
## Cài đặt

Clone project:
```bash
git clone https://github.com/thanhdathd/adr57-node-server.git
cd adr57-node-server
```
Cài dependencies:
```bash
npm install
```
Chạy server:
```bash
npm start
```

